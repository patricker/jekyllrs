[package]
name = "jekyll-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
magnus = { version = "0.6", default-features = false, features = ["rb-sys"] }
once_cell = "1.19"
regex = "1.11"
deunicode = "1.4"
percent-encoding = "2.3"
globset = "0.4"
